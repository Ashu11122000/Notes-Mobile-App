package com.example.notes_app

import io.flutter.embedding.android.FlutterActivity

/**
 * Main Android entry point for the Flutter application.
 *
 * Using the default FlutterActivity provides:
 * - Flutter Embedding V2
 * - Fast application startup
 * - Minimal memory overhead
 * - Full compatibility with Flutter Stable
 *
 * Add native Android code here only when required by a specific plugin.
 */
class MainActivity : FlutterActivity()